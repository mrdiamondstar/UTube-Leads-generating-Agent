# Shell scripts run inside Linux containers: CRLF endings make the kernel fail
# to find the interpreter, reported as a confusing "no such file or directory".
# Force LF regardless of the contributor's platform or core.autocrlf setting.
*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
